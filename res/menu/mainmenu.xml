<menu xmlns:android="http://schemas.android.com/apk/res/android">

  <item
    android:id="@+id/rebuild"
    android:title="@string/rebuild"
    android:showAsAction="ifRoom" android:icon="@drawable/ic_menu_refresh"/>

  <item
    android:id="@+id/prefs"
    android:title="@string/prefs"
    android:showAsAction="ifRoom" android:icon="@drawable/ic_menu_preferences"/>

  <item
    android:id="@+id/showLicenses"
    android:title="@string/licenses"
    android:showAsAction="never" />

</menu>
